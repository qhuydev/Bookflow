import { Business, ProtectedRequest } from './contracts'
export const businessesApi = { list: (request: ProtectedRequest) => request<Business[]>('/api/v1/businesses'), get: (request: ProtectedRequest, id: string) => request<Business>(`/api/v1/businesses/${id}`), create: (request: ProtectedRequest, body: { name: string; slug: string; type: string; timeZone: string }) => request<Business>('/api/v1/businesses', { method: 'POST', body: JSON.stringify(body) }), update: (request: ProtectedRequest, id: string, body: Partial<Pick<Business, 'name'|'slug'|'type'|'timeZone'|'currencyCode'|'cancellationPolicy'|'maxBookingAdvanceDays'>>) => request<Business>(`/api/v1/businesses/${id}`, { method: 'PATCH', body: JSON.stringify(body) }) }
